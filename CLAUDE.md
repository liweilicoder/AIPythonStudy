# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- Python 3.14 virtualenv at `.venv/`
- Run scripts: `python main.py` or `.venv/bin/python main.py`

## Code Style

- Use `if __name__ == "__main__":` guard with a `main()` function entry point
